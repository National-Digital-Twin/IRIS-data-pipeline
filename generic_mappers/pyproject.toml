[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "generic_mappers"
version = "0.1.0"
description = "A generic set of mappers that can be implemented with a variety of sources and destinations."
requires-python = ">=3.9"
